$kind: http-request
description: |-
  Negative test -- tries to create a user with an already-registered email.

  **Expected:** 409 Conflict (duplicate email)
url: "{{user_service}}/api/users"
method: POST
body:
  type: json
  content: |-
    {
      "name": "Otro",
      "lastName": "Usuario",
      "email": "lucas@tarrobuild.cl",
      "phone": "+56911111111"
    }
order: 8000

$kind: http-request
description: |-
  Negative test -- sends null `userId` and empty `name`.

  **Expected:** 400 Bad Request (validation error)
url: "{{build_service}}/api/builds"
method: POST
body:
  type: json
  content: |-
    {
      "userId": null,
      "name": ""
    }
order: 6000

$kind: http-request
description: |-
  Creates a new build for a user. Requires `userId` and `name`.

  **Expected:** 201 Created
url: "{{build_service}}/api/builds"
method: POST
body:
  type: json
  content: |-
    {
      "userId": 1,
      "name": "My New Build"
    }
order: 5000

$kind: http-request
description: |-
  Negative test -- sends quantity = 0 (must be > 0).

  **Expected:** 400 Bad Request (validation error)
url: "{{build_service}}/api/builds/1/items"
method: POST
body:
  type: json
  content: |-
    {
      "productId": 101,
      "quantity": 0
    }
order: 13000

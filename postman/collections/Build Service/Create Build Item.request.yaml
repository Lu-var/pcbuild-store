$kind: http-request
description: |-
  Adds a product item to a build. Requires `productId` and `quantity` (> 0).

  **Expected:** 201 Created
url: "{{build_service}}/api/builds/1/items"
method: POST
body:
  type: json
  content: |-
    {
      "productId": 101,
      "quantity": 1
    }
order: 12000

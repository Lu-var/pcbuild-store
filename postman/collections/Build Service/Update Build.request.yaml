$kind: http-request
description: |-
  Updates an existing build's name. Requires `userId` and `name`.

  **Expected:** 200 OK
url: "{{build_service}}/api/builds/1"
method: PUT
body:
  type: json
  content: |-
    {
      "userId": 1,
      "name": "Updated Build Name"
    }
order: 7000
